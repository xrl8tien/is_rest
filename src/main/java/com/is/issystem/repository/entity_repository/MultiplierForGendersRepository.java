package com.is.issystem.repository.entity_repository;

import com.is.issystem.entities.MultiplierForGenders;
import org.springframework.data.jpa.repository.JpaRepository;

public interface MultiplierForGendersRepository extends JpaRepository<MultiplierForGenders,Integer> {
}
